{
  "name": "snaprevert",
  "version": "0.1.0",
  "description": "The undo button for AI-assisted coding. Auto-snapshots your project, instant rollback.",
  "bin": {
    "snaprevert": "./bin/snaprevert.js"
  },
  "scripts": {
    "test": "jest --verbose",
    "test:unit": "jest --testPathPattern=unit --verbose",
    "test:integration": "jest --testPathPattern=integration --verbose",
    "test:uat": "jest --testPathPattern=uat --verbose",
    "test:coverage": "jest --coverage --verbose"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000,
    "coverageThreshold": {
      "global": {
        "branches": 80,
        "functions": 85,
        "lines": 85,
        "statements": 85
      }
    }
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "chokidar": "^3.6.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
